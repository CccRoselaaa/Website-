# Website
Website Creation using Flask, in Python


Requirements:
Need to install:
    pip install flask
    pip install flask-sqlalchemy

Need to create a connection to MySQL
After creating the database -> running the create database file, assuming there was no error,
then 